// This is a placeholder since we're using the existing hooks/use-toast.ts from shadcn
// If it doesn't exist, we'd need to create it
import { useToast as useToastOriginal } from "@/components/ui/use-toast"

export const useToast = useToastOriginal
